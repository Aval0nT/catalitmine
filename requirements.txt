# Core pipeline dependencies (curated, platform-neutral).
# Install:  pip install -r requirements.txt   (or: bash setup_env.sh)
#
# The legacy CDE2 evidence track (chunk_review_pdf / enrich_chunks_cde /
# extract_records_cde) has its own heavier stack — see requirements-cde2.txt
# and `bash setup_env.sh --with-cde2`.

# PDF parsing: tables (TableFormer) + figure crops + captions
docling>=2.0

# LLM steps only (Phase 0 relevance scoring, prose evidence, vision charts).
# The table track runs without an API key.
anthropic>=0.40

# Analysis / validation
pandas>=2.0
numpy>=1.24
matplotlib>=3.7

# Figure track: bar + line/scatter readers are numpy+Pillow+SciPy; tick-label
# OCR needs pytesseract AND the tesseract binary on PATH
# (brew install tesseract / apt install tesseract-ocr)
pillow>=10.0
scipy>=1.10
pytesseract>=0.3.10

# Reference-page text extraction (provenance resolver)
pdfplumber>=0.11

# Legacy OpenAlex CSV search CLI
requests>=2.31
